# 上线前全局审计摘要

日期：2026-07-28

## 范围

- 检查 React/Vite 前端、编辑器运行时、本地编辑服务、Vercel 配置、发布内容和静态资源。
- 使用本地开发预览 `http://127.0.0.1:5173/` 验证后台页面树、预览切换、媒体状态和 390x844 响应式布局。
- 保留工作区原有未提交改动，仅补充本轮页面映射和审计修正。

## 已验证正常

- `npm.cmd run typecheck` 通过。
- `npm.cmd run verify:content` 通过：2 个画廊模块、11 个插入位、35 个覆盖项，无重复 ID 或无效内容引用。
- `npm.cmd run build` 和 `npm.cmd run verify:build` 通过。
- 后台左侧页面树与用户端窗口一致：例图画廊、价格与活动、联系方式；完整例图作为例图画廊的展开子页面。
- 价格与活动、完整例图、联系方式切换后，预览地址和实际内容均正确。
- 图片背景状态下背景图片可见，背景视频元素隐藏且没有 `src`，不会触发视频加载；只有后台明确配置视频时才由视频逻辑接管。
- 无 BGM 时预览中没有音频元素和音量控件。
- 390x844 下页面树没有横向溢出，按钮均在视口内；全新浏览器页面回归没有 error/warning。
- 根地址重新进入后回到例图画廊，画廊移动端原生滚动和自动滚动逻辑保留。

## 运行边界与残余风险

- 后台 API 由本地 `editor-server.mjs` 提供，线上只读取已发布的 `public/editor-content.json`；线上不能直接使用本地编辑器接口。
- 公开页面仍包含较大的图片和可选视频资源。当前图片背景不会加载视频；用户配置视频时，建议继续使用压缩后的 H.264 MP4，并保留移动端低优先级策略。
- React Router future flags 已启用，后台预览 iframe 消息已限制为同源及父子窗口；当前没有新增运行时残余风险。
